import React from 'react';
import Button from '../src/Button';
console.log(Button)

export default { title: 'Index' };

export const withText = () => <Button />;
export const withEmoji2 = () => <Button />;
